import styled from "styled-components";

const LoginForm = styled.div`
    & {
        width: 382px;
        overflow: hidden;
        margin: auto;
        margin: 20 0 0 450px;
        padding: 80px;
        background: #e1e1eb;
        border-radius: 15px;
    }

    .login-form-button {
        width: 100%;
    }

    .login-logo {
        display: inline-block;
    }
`;
export const SC = { LoginForm };
