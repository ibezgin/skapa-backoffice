import styled from "styled-components";

const Date = styled.div`
    white-space: nowrap;
`;

const Time = styled.div`
    font-size: 11px;
    line-height: 12px;
    color: #617279;
    display: block;
`;

export const SC = { Time, Date };
