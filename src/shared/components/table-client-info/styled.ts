import styled from "styled-components";

const Name = styled.div`
    white-space: nowrap;
`;

const Phone = styled.div`
    font-size: 11px;
    line-height: 12px;
    color: #617279;
    display: block;
`;

export const SC = { Name, Phone };
