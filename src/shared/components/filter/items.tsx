import { FilterAssignedToMe } from "./assigned-to-me";
import { FilterClientId } from "./clientId";
import { FilterPeriods } from "./periods";

export const filterItems = {
    FilterAssignedToMe,
    FilterClientId,
    FilterPeriods,
};
